framework:
    cache:
        # Put the unique name of your application here: the prefix seed
        # is used to compute stable namespaces for cache keys.
        #prefix_seed: your_vendor_name/app_name

        # The application cache caches to the filesystem by default.
        # Other options include:

        # Redis
        #application: cache.adapter.redis
        #default_redis_provider: redis://localhost

        # APCu (not recommended with heavy random-write workloads as memory fragmentation can cause perf issues)
        #application: cache.adapter.apcu

        # Namespaced pools use the above "application" backend by default
        #pools:
            #my.dedicated.cache: ~
