{% extends 'base.html.twig' %}

{% block title %}Edit Seizure{% endblock %}

{% block body %}
    <h1>Edit Seizure</h1>

    {{ include('seizure/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('seizure_index') }}">back to list</a>

    {{ include('seizure/_delete_form.html.twig') }}
{% endblock %}
