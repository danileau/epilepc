{{ form_start(form) }}
    {{ form_errors(form) }}
    {{ form_row(form.title, {'label': 'Anfallstitel'}) }}
    {{ form_row(form.description, {'label': 'Beschreibung'}) }}
    {{ form_row(form.timestamp_when, {'label': 'Anfallszeitpunkt'}) }}
    {{ form_row(form.seizuretype, {'label': 'Anfallstyp'}) }}
    <button class="btn btn-primary">{{ button_text }}</button>
{{ form_end(form) }}