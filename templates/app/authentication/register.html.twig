{% extends 'app/authentication/base.html.twig' %}
{% form_theme registrationForm _self %}

{% block title %}Epilepc - Registrieren{% endblock %}

{%- block form_row -%}
    {%- set widget_attr = {} -%}
    {%- if help is not empty -%}
        {%- set widget_attr = {attr: {'aria-describedby': id ~"_help"}} -%}
    {%- endif -%}

    {#{{- form_label(form) -}}#}
    {{- form_errors(form) -}}
    {{- form_widget(form, widget_attr) -}}
    {{- form_help(form) -}}

{%- endblock form_row -%}

{% block _user_registration_form_agreeTerms_row %}
    <div class="form-group">
        <div class="custom-control custom-checkbox small">
            {{ form_errors(form) }}
            <input type="checkbox" name="{{ full_name }}" class="custom-control-input" id="customCheck">
            <label class="custom-control-label" for="customCheck">Ich stimme den <a href="#">Nutzungsbedingungen</a> von epilepc.ch zu</label>
        </div>
    </div>
{% endblock %}

{% block body %}
    <!-- Nested Row within Card Body -->
    <div class="row">
        <div class="col-lg-5 d-none d-lg-block bg-register-image"></div>
        <div class="col-lg-7">
            <div class="p-5">
                <div class="text-center">
                    <h1 class="h4 text-gray-900 mb-4">Registriere dich jetzt!</h1>
                </div>

                {{ form_start(registrationForm, {
                    attr: { class: 'user' }
                }) }}
                <div class="form-group row">
                    <div class="col-sm-6 mb-3 mb-sm-0">
                        {{ form_row(registrationForm.firstname, {
                            attr: { class: 'form-control-user', placeholder: 'Vorname'}
                        }) }}
                    </div>
                    <div class="col-sm-6">
                        {{ form_row(registrationForm.lastname, {
                            attr: { class: 'form-control-user', placeholder: 'Nachname'}
                        }) }}
                    </div>
                </div>
                <div class="form-group">
                    {{ form_row(registrationForm.email, {
                        attr: { class: 'form-control-user', placeholder: 'Email-Adresse'}
                    }) }}
                </div>
                <div class="form-group">
                    {{ form_row(registrationForm.plainPassword, {
                        attr: { class: 'form-control-user', placeholder: 'Passwort'}
                    }) }}
                </div>

                {{ form_row(registrationForm.agreeTerms) }}


                    <button type="submit" class="btn btn-primary btn-user btn-block">Account erstellen</button>
                {{ form_end(registrationForm) }}

                {#
                <form class="user" method="post">
                    <div class="form-group row">
                        <div class="col-sm-6 mb-3 mb-sm-0">
                            <input type="text" class="form-control form-control-user" id="FirstName" name="firstname" placeholder="Vorname">
                        </div>
                        <div class="col-sm-6">
                            <input type="text" class="form-control form-control-user" id="LastName" name="lastname" placeholder="Nachname">
                        </div>
                    </div>
                    <div class="form-group">
                        <input type="email" class="form-control form-control-user" id="exampleInputEmail" name="email" placeholder="Email Adresse">
                    </div>
                    <div class="form-group">
                        <input type="password" class="form-control form-control-user" id="InputPassword" name="inputPassword" placeholder="Passwort">
                    </div>

                    <div class="form-group">
                        <div class="custom-control custom-checkbox small">
                            <input type="checkbox" name="_terms" class="custom-control-input" id="customCheck">
                            <label class="custom-control-label" for="customCheck">Ich stimme den Nutzungsbedingungen von epilepc.ch zu</label>
                        </div>
                    </div>
                    <button type="submit" class="btn btn-primary btn-user btn-block">Account erstellen</button>

                </form>#}
                <hr>
                <div class="text-center">
                    <a class="small" href="{{ path('app_forgot') }}">Passwort vergessen?</a>
                </div>
                <div class="text-center">
                    <a class="small" href="{{ path('app_login') }}">Account bereits erstellt? Log dich jetzt ein!</a>
                </div>
                <br />
                <div class="text-center">
                    <a class="small" href="{{ path('app_landingpage') }}">Zurück</a>
                </div>
            </div>
        </div>
    </div>

{% endblock %}