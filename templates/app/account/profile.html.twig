
{% extends 'app/base.html.twig' %}

{% block body %}
<div class="container">
    <!-- Page Heading -->
    <div class="mb-5">
        <div class="text-center">
            <h1 class="h3 text-gray-800 justify-content-center border-bottom pb-2 mb-2">Profilübersicht von {{ app.user.firstname }}</h1>
            <p class="mb-4">Hier findest du alle Informationen über dein Profil und kannst Sie gleich bearbeiten</p>
        </div>
    </div>

    <!-- DataTales Example -->
<div class="row">
    <!-- Area Chart -->
    <div class="col-xl-12 col-lg-7" id="AreaChart">
        <div class="card shadow mb-4">
        <div class="card-header py-3">
            <h6 class="m-0 font-weight-bold text-primary">Profilinformationen</h6>
        </div>
        <div class="card-body">

            <div class="row">
                <div class="col-md-12 order-md-1">
                    {{ include('app/account/_form.html.twig', {'button_text': 'Speichern'}) }}
                    <br/>
                    <a class="btn btn-dark" href="{{ path('app_dashboard') }}">Zurück zum Dashboard</a>
                </div>
            </div>
        </div>
    </div>
    </div>
</div>
</div>
{% endblock %}
