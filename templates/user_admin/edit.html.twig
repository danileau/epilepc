{% extends 'app/base.html.twig' %}

{% block body %}
<!-- Page Heading -->
<div class="mb-5">
    <div class="text-center">
        <h1 class="h3 text-gray-800 justify-content-center border-bottom pb-2 mb-2">Benutzer #{{ user.id }} bearbeiten</h1>
    </div>


    <!-- DataTales Example -->
    <div class="card shadow mb-4">
        <div class="card-body">
            <div class="table-responsive">
                {{ include('user_admin/_form.html.twig', {'button_text': 'Updaten'}) }}
                <br/>

                <a class="btn btn-dark" href="{{ path('admin_user_index') }}">Zurück zur Übersicht</a>

            </div>
        </div>
    </div>
{% endblock %}
